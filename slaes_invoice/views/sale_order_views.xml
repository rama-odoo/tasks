<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <!--INHERIT  IT - sale-->
    <!-- <record id="view_partner_form_inherit_sale_order_views" model="ir.ui.view">
        <field name="name">view.partner.form.inherit.sale.order.views</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="	sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook[last()]" position="inside">
                <page string="Inventory" name="par_location">
                    <field name="product_uom_detail_ids">
                        <tree editable='top'>
                            <field name="product_id"/>
                            <field name="uom_id" domain="[('id','in',available_uom_ids)]"/>
                            <field name="available_uom_ids" invisible="1"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record> -->
</odoo>
           